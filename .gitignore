# Python
__pycache__/
*.pyc
.venv/

# macOS
.DS_Store

# Generated output (synthetic images, debug renders, demo plots, etc.)
# NOTE: figure names are anchored with a leading "/" so they only ignore the
# regenerated copies at the repo root. Committed copies under docs/assets/
# (used by the website + paper) are deliberately NOT ignored.
synth_out/
debug_out/
/rc_charging.png
/rectifier.png
/rlc_ringing.png
/noise_robustness.png
/extractor_ablation.png
/difficulty_curve.png
/bode_lowpass.png
/bode_bandpass.png
/bode_diode_tunable.png

# Phase 1: raw dataset, prepped YOLO dataset, training runs, model weights
# (large/binary — keep them out of git; see training/COLAB_INSTRUCTIONS.md)
cghd_yolo/
cghd_raw/
runs/
training/weights/
*.pt

# Editor
.vscode/

# Local Claude Code config (machine-specific permission allowlist — not for the public repo)
.claude/settings.local.json

# Local-only working notes (never publish)
PUBLISHING.md
CLEANUP_TODO.md
